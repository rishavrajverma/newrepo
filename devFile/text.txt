Stations::V4webviewerV4 &
"<!DOCTYPE html>
  <html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>JSON Data Tables</title>
    <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet' />
    <link href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css' rel='stylesheet' />
   



  <style>
      body {
        margin: 0;
        padding: 0;
        min-height: 500px;
      }

      #accordionData {
        margin: 10px 10px;
      }

      .accordion-item {
        border-radius: 0.75rem;
        margin-bottom: 1.5rem;
        overflow: hidden;
        border: 1px solid #dee2e6;
      }
      .accordion-header {
        position: relative;
      }

      .center-scroll-buttons {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 3;
      }
      .accordion-button {
        font-weight: 600;
        cursor: pointer;
        box-shadow: none !important;
        height: auto;
        color: #3d3d3d !important;
        font-size: 18px;
        background-color: #f8f9fa !important;
        margin: -1px;
        z-index: 2;
        position: relative;
      }
      .accordion-button::after {
        margin-left: auto;
        margin-right: 20px;
      }
      .accordion-num {
        color: #7a7a7a;
      }

      .scroll-btn {
        background: transparent;
        border: none;
        z-index: 5;
        width: 30px;
        height: 40px;
        margin-left: 2px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .accordion-button.collapsed {
        border-radius: 0.75rem !important;
        border: 1px solid #f8f9fa;
      }

      .accordion-button:not(.collapsed) {
        border-bottom-left-radius: 0 !important;
        border-bottom-right-radius: 0 !important;
      }

      .accordion-body {
        padding: 0%;
        background-color: #fff;
      }

      .table-wrapper {
        max-height: 200px;
        overflow-y: auto;
        overflow-x: hidden;
      }

      .table-horizontal-scroll {
        overflow-x: auto;
        position: relative;
        scrollbar-width: none;
        -ms-overflow-style: none;
      }

      .table-horizontal-scroll::-webkit-scrollbar {
        display: none;
      }

      table {
        width: max-content;
        border-collapse: collapse;
        font-family: Rubik;
      }

      th,
      td {
        padding: 4px 10px;
        text-align: center;
        white-space: nowrap;
        width: 100px;
      }

      th {
        cursor: pointer;
        padding: 12px;
        font-size: 13px;
        font-weight: 600;
        color: #3d3d3d;
        background-color: #f5f5f5;
        position: sticky;
        top: 0;
        z-index: 1;
      }
      td {
        font-size: 12px;
        font-weight: 500;
      }
      tbody > tr {
      }

      th i {
        margin-left: 5px;
        cursor: pointer;
        font-size: 0.8em;
        color: #888;
      }

      .sticky-col {
        position: sticky;
        background-color: white;
        z-index: 2;
        box-shadow: 2px 0 5px -2px rgba(0, 0, 0, 0.1);
      }

      .sticky-col-1 {
        left: 0;
        z-index: 3;
      }

      .sticky-col-2 {
        left:110px;
      }

      .sticky-col-3 {
        left: 200px;
      }

      .sticky-col-4 {
        left: 300px;
      }

      .sticky-col-5 {
        left: 400px;
      }
      .sticky-col-6 {
        left: 500px;
      }  
    </style>
 
  








  </head>
  <body>
  <div  id='main'>
    <div class='accordion' id='accordionData'></div>
   

 
<script>
 const jsonData = " & $$tableJSON & ";

const groupedByCountry = {};
  for (const station in jsonData) {
    const entry = jsonData[station];
    const country = entry.country;
    if (!groupedByCountry[country]) groupedByCountry[country] = [];
    groupedByCountry[country].push({ station, ...entry });
  }
console.log(groupedByCountry);
  const accordion = document.getElementById('accordionData');
  let index = 0;

  for (const country in groupedByCountry) {
    const data = groupedByCountry[country];
    const keys = Object.keys(data[0]).filter((k) => k !== 'country');

    const preferredOrder = [
      'station','city','caterer','unresolvedCount','closedCount','totalCount',
      'Delays','Quality','Compliance','Provisioning','Unclassified','Safety','actions'
    ];

    const sampleEntry = Object.values(groupedByCountry)[0]?.[0] || {};
    const dynamicKeys = Object.keys(sampleEntry).filter(
      (key) => !['country'].concat(preferredOrder).includes(key)
    );

    const finalKeys = preferredOrder.concat(dynamicKeys);
    const sortableKeys = ['station', 'city', 'caterer'];

    const thead = finalKeys.map((key, i) => {
  const stickyClass = i < 6 ? `sticky-col sticky-col-${i + 1}` : '';
  const sortableClass = sortableKeys.includes(key.toLowerCase()) ? 'sortable' : '';
  let label = key.charAt(0).toUpperCase() + key.slice(1);
  if (key === 'station') label = 'Station Code';
  if (key === 'unresolvedCount') label = 'Unresolved';
  if (key === 'closedCount') label = 'Closed';
  if (key === 'totalCount') label = 'Total';
  if (key === 'actions') label = 'Action';

  return `
    <th class='${sortableClass} ${stickyClass}' data-key='${key}' style='width: 50px;'>
      <div class='d-inline-flex align-items-center gap-1 justify-content-center'>
        <span>${label}</span>
        ${
          sortableClass
            ? `<div class='d-flex flex-column'>
                <svg xmlns='http://www.w3.org/2000/svg' height='16' viewBox='0 0 24 24' fill='none' stroke='#888' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'>
                  <polygon points='3 4 21 4 14 12.5 14 19 10 21 10 12.5 3 4'/>
                </svg>
              </div>`
            : ''
        }
      </div>
    </th>`;
}).join('');


    const tbody = data.map((row) => `
<tr>
  ${finalKeys.map((key, i) => {
    const stickyClass = i < 6 ? 'sticky-col sticky-col-' + (i+1) : '';
    let value = row[key];
    if (key === 'actions') {
      value = `<button type='button' class='btn btn-sm'><i class='fa-solid fa-arrow-right' style='transform: rotate(300deg); display: inline-block; color: #641BFD;'></i></button>`;
    }
    return '<td class=\"' + stickyClass + '\">' + (value !== undefined ? value : '') + '</td>';
  }).join('')}
</tr>`).join('');

    const countryId = 'country-' + index;
    const collapseId = 'collapse-' + index;
    const accordionItem = document.createElement('div');
    accordionItem.className = 'accordion-item';
    accordionItem.innerHTML = `





 <h2 class='accordion-header' id='${countryId}'>
  <div class='d-flex align-items-center justify-content-between w-100' style='background-color: #f8f9fa;'>
  <button class='accordion-button d-flex align-items-center gap-3' type='button'
          data-bs-toggle='collapse' data-bs-target='#${collapseId}' aria-controls='${collapseId}'>

    <span class='fw-semibold'>${country}</span>

    <span class='accordion-num text-muted'>(${data.length})</span>
    
  </button>
  <button class='scroll-btn btn btn-sm' type='button' data-direction='right'>
        <i class='fas fa-chevron-right'></i>
      </button>
  </div>

  <div class='center-scroll-buttons d-flex justify-content-between align-items-center'>
  <div class='flex-grow-1 d-flex justify-content-center'>
    <button class='scroll-btn btn btn-sm' type='button' data-direction='left'>
      <i class='fas fa-chevron-left'></i>
    </button>
  </div>

  <div >
 
</div>
 
</div>

</h2>









<div id='${collapseId}' class='accordion-collapse collapse show' aria-labelledby='${countryId}'>
  <div class='accordion-body'>
    <div class='table-wrapper'>
      <div class='table-horizontal-scroll'>
        <table class='sortable-table' data-country='${country}'>
          <thead><tr>${thead}</tr></thead>
          <tbody>${tbody}</tbody>
        </table>
      </div>
    </div>
  </div>
</div>`;
    accordion.appendChild(accordionItem);
    index++;
  }

  document.addEventListener('click', function (e) {
    const th = e.target.closest('th[data-key]');
    if (!th) return;
    const key = th.dataset.key;
    const table = th.closest('table');
    const tbody = table.querySelector('tbody');
    const rows = Array.from(tbody.querySelectorAll('tr'));
    const ascending = th.dataset.order !== 'asc';
    th.dataset.order = ascending ? 'asc' : 'desc';
    const colIndex = Array.from(th.parentNode.children).indexOf(th);
    rows.sort((a, b) => {
      const aText = a.children[colIndex].textContent.trim().toLowerCase();
      const bText = b.children[colIndex].textContent.trim().toLowerCase();
      return ascending ? aText.localeCompare(bText) : bText.localeCompare(aText);
    });
    tbody.innerHTML = '';
    rows.forEach((row) => tbody.appendChild(row));
  });


document.querySelectorAll('.scroll-btn').forEach(button => {
  button.addEventListener('click', function () {
    const direction = this.getAttribute('data-direction');
    scrollTable(this, direction);
  });
});

function scrollTable(button, direction) {
  console.log('Scroll triggered');
  const container = button.closest('.accordion-item').querySelector('.table-horizontal-scroll');
  if (!container) return;
  const scrollAmount = 200;
  container.scrollBy({
    left: direction === 'left' ? -scrollAmount : scrollAmount,
    behavior: 'smooth'
  });
}

document.querySelectorAll('.scroll-btn').forEach(button => {
  button.addEventListener('click', function () {
    const direction = this.getAttribute('data-direction');
    scrollTable(this, direction);
  });
});
  




</script>







    <script src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js'></script>
</div>  
</body>
  </html>"